export const maxWidth = (breakpoint) => `max-width: ${breakpoint + 1}px`;
export const minWidth = (breakpoint) => `min-width: ${breakpoint}px`;

export const BREAKPOINT_DESKTOP = 1024;
export const BREAKPOINT_MOBILE = 414;
